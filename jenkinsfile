pipeline {
  agent any
  stages {
        
    stage('Cloning Git') {
      steps {
        git 'https://github.com/abbeycheche/ansible-code.git'
      }
    }
        
    stage('Install dependencies') {
      steps {
        sh "dnf install 'dnf-command(npm)'"
      }
    }
     
    stage('Test') {
      steps {
         sh 'npm test'
      }
    }      
  }
}
